import DesignSystemDemo from "./demo-client";

export const metadata = {
  title: "Design System Mobile First | VM Advocacia",
  robots: {
    index: false,
    follow: false,
  },
};

export default function DesignSystemPage() {
  return <DesignSystemDemo />;
}
