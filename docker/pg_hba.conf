# Development-only pg_hba.conf — trust all connections
# DO NOT USE IN PRODUCTION

# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             all                                     trust
host    all             all             0.0.0.0/0               trust
host    all             all             ::0/0                   trust
